/**FERC Form 1 Rules 
**/


RULE-NAME-PREFIX FERC

assert F2.302.1 satisfied 
$rule_id = rule-name();

if set('2-A','2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = [@RevenuesForTransitionCostsAndTakeOrPay]; 
<<<<<<< HEAD
 $addend = sum(list([@RevenuesForTransitionCostsAndTakeOrPay @RateScheduleAndZoneOfReceiptAxis=*])#breakdown);
=======
 $addend = sum(list([nonils @RevenuesForTransitionCostsAndTakeOrPay @RateScheduleAndZoneOfReceiptAxis=*])#breakdown);
>>>>>>> origin/2021-01-01
 if $addend == none
 	skip
 else
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The breakdown of the element {$sum1.concept.name} using the axis RateScheduleAndZoneOfReceiptAxis has a total value of {$addend}. The value of the aggregate amount for {$sum1.concept.name} is equal to {$sum1}.  The disaggregated amounts are {$breakdown.join(' + ')}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.302.2 satisfied 
$rule_id = rule-name();

if set('2-A','2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = [@RevenuesFromGRIAndACA]; 
<<<<<<< HEAD
 $addend = sum(list([@RevenuesFromGRIAndACA @RateScheduleAndZoneOfReceiptAxis=*])#breakdown);
=======
 $addend = sum(list([nonils @RevenuesFromGRIAndACA @RateScheduleAndZoneOfReceiptAxis=*])#breakdown);
>>>>>>> origin/2021-01-01
 if $addend == none
 	skip
 else
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The breakdown of the element {$sum1.concept.name} using the axis RateScheduleAndZoneOfReceiptAxis has a total value of {$addend}. The value of the aggregate amount for {$sum1.concept.name} is equal to {$sum1}.  The disaggregated amounts are {$breakdown.join(' + ')}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.302.3 satisfied 
$rule_id = rule-name();

if set('2-A','2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = [@OtherRevenues]; 
<<<<<<< HEAD
 $addend = sum(list([@OtherRevenues @RateScheduleAndZoneOfReceiptAxis=*])#breakdown);
=======
 $addend = sum(list([nonils @OtherRevenues @RateScheduleAndZoneOfReceiptAxis=*])#breakdown);
>>>>>>> origin/2021-01-01
 if $addend == none
 	skip
 else
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The breakdown of the element {$sum1.concept.name} using the axis RateScheduleAndZoneOfReceiptAxis has a total value of {$addend}. The value of the aggregate amount for {$sum1.concept.name} is equal to {$sum1}.  The disaggregated amounts are {$breakdown.join(' + ')}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.302.4 satisfied 
$rule_id = rule-name();

if set('2-A','2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = [@RevenuesFromTransportationOfGasOfOthersThroughGatheringFacilities]; 
<<<<<<< HEAD
 $addend = sum(list([@RevenuesFromTransportationOfGasOfOthersThroughGatheringFacilities @RateScheduleAndZoneOfReceiptAxis=*])#breakdown);
=======
 $addend = sum(list([nonils @RevenuesFromTransportationOfGasOfOthersThroughGatheringFacilities @RateScheduleAndZoneOfReceiptAxis=*])#breakdown);
>>>>>>> origin/2021-01-01
 if $addend == none
 	skip
 else
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The breakdown of the element {$sum1.concept.name} using the axis RateScheduleAndZoneOfReceiptAxis has a total value of {$addend}. The value of the aggregate amount for {$sum1.concept.name} is equal to {$sum1}.  The disaggregated amounts are {$breakdown.join(' + ')}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.302.5 satisfied 
$rule_id = rule-name();

if set('2-A','2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = [@DekathermOfNaturalGas]; 
<<<<<<< HEAD
 $addend = sum(list([@DekathermOfNaturalGas @RateScheduleAndZoneOfReceiptAxis=*])#breakdown);
=======
 $addend = sum(list([nonils @DekathermOfNaturalGas @RateScheduleAndZoneOfReceiptAxis=*])#breakdown);
>>>>>>> origin/2021-01-01
 if $addend == none
 	skip
 else
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The breakdown of the element {$sum1.concept.name} using the axis RateScheduleAndZoneOfReceiptAxis has a total value of {$addend}. The value of the aggregate amount for {$sum1.concept.name} is equal to {$sum1}.  The disaggregated amounts are {$breakdown.join(' + ')}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1
