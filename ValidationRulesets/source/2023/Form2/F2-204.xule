/**FERC Form 2 Rules 
**/


RULE-NAME-PREFIX FERC

assert F2.204.1 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@Organization @period=$currentInstant}; 
 $addend = ({@Organization @period=$priorInstant}
 + {@OrganizationAdditions @period=$currentDuration}
 - {@OrganizationRetirements @period=$currentDuration}
 + {@OrganizationAdjustments @period=$currentDuration}
 + {@OrganizationTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.2 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@FranchisesAndConsents @period=$currentInstant}; 
 $addend = ({@FranchisesAndConsents @period=$priorInstant}
 + {@FranchisesAndConsentsAdditions @period=$currentDuration}
 - {@FranchisesAndConsentsRetirements @period=$currentDuration}
 + {@FranchisesAndConsentsAdjustments @period=$currentDuration}
 + {@FranchisesAndConsentsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.3 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MiscellaneousIntangiblePlant @period=$currentInstant}; 
 $addend = ({@MiscellaneousIntangiblePlant @period=$priorInstant}
 + {@MiscellaneousIntangiblePlantAdditions @period=$currentDuration}
 - {@MiscellaneousIntangiblePlantRetirements @period=$currentDuration}
 + {@MiscellaneousIntangiblePlantAdjustments @period=$currentDuration}
 + {@MiscellaneousIntangiblePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.4 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@IntangiblePlant}; 
 $addend = ({@Organization}
 + {@FranchisesAndConsents}
 + {@MiscellaneousIntangiblePlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.5 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@ProducingLands @period=$currentInstant}; 
 $addend = ({@ProducingLands @period=$priorInstant}
 + {@ProducingLandsAdditions @period=$currentDuration}
 - {@ProducingLandsRetirements @period=$currentDuration}
 + {@ProducingLandsAdjustments @period=$currentDuration}
 + {@ProducingLandsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.6 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@ProducingLeaseholds @period=$currentInstant}; 
 $addend = ({@ProducingLeaseholds @period=$priorInstant}
 + {@ProducingLeaseholdsAdditions @period=$currentDuration}
 - {@ProducingLeaseholdsRetirements @period=$currentDuration}
 + {@ProducingLeaseholdsAdjustments @period=$currentDuration}
 + {@ProducingLeaseholdsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.7 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@GasRights @period=$currentInstant}; 
 $addend = ({@GasRights @period=$priorInstant}
 + {@GasRightsAdditions @period=$currentDuration}
 - {@GasRightsRetirements @period=$currentDuration}
 + {@GasRightsAdjustments @period=$currentDuration}
 + {@GasRightsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.8 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@RightsOfWayNaturalGasProductionAndGatheringPlant @period=$currentInstant}; 
 $addend = ({@RightsOfWayNaturalGasProductionAndGatheringPlant @period=$priorInstant}
 + {@RightsOfWayNaturalGasProductionAndGatheringPlantAdditions @period=$currentDuration}
 - {@RightsOfWayNaturalGasProductionAndGatheringPlantRetirements @period=$currentDuration}
 + {@RightsOfWayNaturalGasProductionAndGatheringPlantAdjustments @period=$currentDuration}
 + {@RightsOfWayNaturalGasProductionAndGatheringPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.9 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherLandAndLandRights @period=$currentInstant}; 
 $addend = ({@OtherLandAndLandRights @period=$priorInstant}
 + {@OtherLandAndLandRightsAdditions @period=$currentDuration}
 - {@OtherLandAndLandRightsRetirements @period=$currentDuration}
 + {@OtherLandAndLandRightsAdjustments @period=$currentDuration}
 + {@OtherLandAndLandRightsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.10 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@GasWellStructures @period=$currentInstant}; 
 $addend = ({@GasWellStructures @period=$priorInstant}
 + {@GasWellStructuresAdditions @period=$currentDuration}
 - {@GasWellStructuresRetirements @period=$currentDuration}
 + {@GasWellStructuresAdjustments @period=$currentDuration}
 + {@GasWellStructuresTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.11 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@FieldCompressorStationStructures @period=$currentInstant}; 
 $addend = ({@FieldCompressorStationStructures @period=$priorInstant}
 + {@FieldCompressorStationStructuresAdditions @period=$currentDuration}
 - {@FieldCompressorStationStructuresRetirements @period=$currentDuration}
 + {@FieldCompressorStationStructuresAdjustments @period=$currentDuration}
 + {@FieldCompressorStationStructuresTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.12 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@FieldMeasuringAndRegulatingStationStructures @period=$currentInstant}; 
 $addend = ({@FieldMeasuringAndRegulatingStationStructures @period=$priorInstant}
 + {@FieldMeasuringAndRegulatingStationStructureAdditions @period=$currentDuration}
 - {@FieldMeasuringAndRegulatingStationStructureRetirements @period=$currentDuration}
 + {@FieldMeasuringAndRegulatingStationStructureAdjustments @period=$currentDuration}
 + {@FieldMeasuringAndRegulatingStationStructureTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.


Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.13 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherStructures @period=$currentInstant}; 
 $addend = ({@OtherStructures @period=$priorInstant}
 + {@OtherStructuresAdditions @period=$currentDuration}
 - {@OtherStructuresRetirements @period=$currentDuration}
 + {@OtherStructuresAdjustments @period=$currentDuration}
 + {@OtherStructuresTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.14 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@ProducingGasWellsWellConstruction @period=$currentInstant}; 
 $addend = ({@ProducingGasWellsWellConstruction @period=$priorInstant}
 + {@ProducingGasWellsWellConstructionAdditions @period=$currentDuration}
 - {@ProducingGasWellsWellConstructionRetirements @period=$currentDuration}
 + {@ProducingGasWellsWellConstructionAdjustments @period=$currentDuration}
 + {@ProducingGasWellsWellConstructionTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.15 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@ProducingGasWellsWellEquipment @period=$currentInstant}; 
 $addend = ({@ProducingGasWellsWellEquipment @period=$priorInstant}
 + {@ProducingGasWellsWellEquipmentAdditions @period=$currentDuration}
 - {@ProducingGasWellsWellEquipmentRetirements @period=$currentDuration}
 + {@ProducingGasWellsWellEquipmentAdjustments @period=$currentDuration}
 + {@ProducingGasWellsWellEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.16 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@FieldLines @period=$currentInstant}; 
 $addend = ({@FieldLines @period=$priorInstant}
 + {@FieldLinesAdditions @period=$currentDuration}
 - {@FieldLinesRetirements @period=$currentDuration}
 + {@FieldLinesAdjustments @period=$currentDuration}
 + {@FieldLinesTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.17 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@FieldCompressorStationEquipment @period=$currentInstant}; 
 $addend = ({@FieldCompressorStationEquipment @period=$priorInstant}
 + {@FieldCompressorStationEquipmentAdditions @period=$currentDuration}
 - {@FieldCompressorStationEquipmentRetirements @period=$currentDuration}
 + {@FieldCompressorStationEquipmentAdjustments @period=$currentDuration}
 + {@FieldCompressorStationEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.18 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@FieldMeasuringAndRegulatingStationEquipment @period=$currentInstant}; 
 $addend = ({@FieldMeasuringAndRegulatingStationEquipment @period=$priorInstant}
 + {@FieldMeasuringAndRegulatingStationEquipmentAdditions @period=$currentDuration}
 - {@FieldMeasuringAndRegulatingStationEquipmentRetirements @period=$currentDuration}
 + {@FieldMeasuringAndRegulatingStationEquipmentAdjustments @period=$currentDuration}
 + {@FieldMeasuringAndRegulatingStationEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.


Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.19 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@DrillingAndCleaningEquipment @period=$currentInstant}; 
 $addend = ({@DrillingAndCleaningEquipment @period=$priorInstant}
 + {@DrillingAndCleaningEquipmentAdditions @period=$currentDuration}
 - {@DrillingAndCleaningEquipmentRetirements @period=$currentDuration}
 + {@DrillingAndCleaningEquipmentAdjustments @period=$currentDuration}
 + {@DrillingAndCleaningEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.20 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@PurificationEquipmentNaturalGasProductionPlant @period=$currentInstant}; 
 $addend = ({@PurificationEquipmentNaturalGasProductionPlant @period=$priorInstant}
 + {@PurificationEquipmentNaturalGasProductionPlantAdditions @period=$currentDuration}
 - {@PurificationEquipmentNaturalGasProductionPlantRetirements @period=$currentDuration}
 + {@PurificationEquipmentNaturalGasProductionPlantAdjustments @period=$currentDuration}
 + {@PurificationEquipmentNaturalGasProductionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.


Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.21 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherEquipmentNaturalGasProductionAndGatheringPlant @period=$currentInstant}; 
 $addend = ({@OtherEquipmentNaturalGasProductionAndGatheringPlant @period=$priorInstant}
 + {@OtherEquipmentNaturalGasProductionAndGatheringPlantAdditions @period=$currentDuration}
 - {@OtherEquipmentNaturalGasProductionAndGatheringPlantRetirements @period=$currentDuration}
 + {@OtherEquipmentNaturalGasProductionAndGatheringPlantAdjustments @period=$currentDuration}
 + {@OtherEquipmentNaturalGasProductionAndGatheringPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.


Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.22 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@UnsuccessfulExplorationAndDevelopmentCosts @period=$currentInstant}; 
 $addend = ({@UnsuccessfulExplorationAndDevelopmentCosts @period=$priorInstant}
 + {@UnsuccessfulExplorationAndDevelopmentCostsAdditions @period=$currentDuration}
 - {@UnsuccessfulExplorationAndDevelopmentCostsRetirements @period=$currentDuration}
 + {@UnsuccessfulExplorationAndDevelopmentCostsAdjustments @period=$currentDuration}
 + {@UnsuccessfulExplorationAndDevelopmentCostsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.


Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.23 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@AssetRetirementCostsForNaturalGasProductionAndGatheringPlant @period=$currentInstant}; 
 $addend = ({@AssetRetirementCostsForNaturalGasProductionAndGatheringPlant @period=$priorInstant}
 + {@AssetRetirementCostsForNaturalGasProductionAndGatheringPlantAdditions @period=$currentDuration}
 - {@AssetRetirementCostsForNaturalGasProductionAndGatheringPlantRetirements @period=$currentDuration}
 + {@AssetRetirementCostsForNaturalGasProductionAndGatheringPlantAdjustments @period=$currentDuration}
 + {@AssetRetirementCostsForNaturalGasProductionAndGatheringPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.24 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@NaturalGasProductionAndGatheringPlant}; 
 $addend = ({@ProducingLands}
 + {@ProducingLeaseholds}
 + {@GasRights}
 + {@RightsOfWayNaturalGasProductionAndGatheringPlant}
 + {@OtherLandAndLandRights}
 + {@GasWellStructures}
 + {@FieldCompressorStationStructures}
 + {@FieldMeasuringAndRegulatingStationStructures}
 + {@OtherStructures}
 + {@ProducingGasWellsWellConstruction}
 + {@ProducingGasWellsWellEquipment}
 + {@FieldLines}
 + {@FieldCompressorStationEquipment}
 + {@FieldMeasuringAndRegulatingStationEquipment}
 + {@DrillingAndCleaningEquipment}
 + {@PurificationEquipmentNaturalGasProductionPlant}
 + {@OtherEquipmentNaturalGasProductionAndGatheringPlant}
 + {@UnsuccessfulExplorationAndDevelopmentCosts}
 + {@AssetRetirementCostsForNaturalGasProductionAndGatheringPlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.25 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@LandAndLandRightsProductsExtractionPlant @period=$currentInstant}; 
 $addend = ({@LandAndLandRightsProductsExtractionPlant @period=$priorInstant}
 + {@LandAndLandRightsProductsExtractionPlantAdditions @period=$currentDuration}
 - {@LandAndLandRightsProductsExtractionPlantRetirements @period=$currentDuration}
 + {@LandAndLandRightsProductsExtractionPlantAdjustments @period=$currentDuration}
 + {@LandAndLandRightsProductsExtractionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.26 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@StructuresAndImprovementsProductsExtractionPlant @period=$currentInstant}; 
 $addend = ({@StructuresAndImprovementsProductsExtractionPlant @period=$priorInstant}
 + {@StructuresAndImprovementsProductsExtractionPlantAdditions @period=$currentDuration}
 - {@StructuresAndImprovementsProductsExtractionPlantRetirements @period=$currentDuration}
 + {@StructuresAndImprovementsProductsExtractionPlantAdjustments @period=$currentDuration}
 + {@StructuresAndImprovementsProductsExtractionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.27 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@ExtractionAndRefiningEquipment @period=$currentInstant}; 
 $addend = ({@ExtractionAndRefiningEquipment @period=$priorInstant}
 + {@ExtractionAndRefiningEquipmentAdditions @period=$currentDuration}
 - {@ExtractionAndRefiningEquipmentRetirements @period=$currentDuration}
 + {@ExtractionAndRefiningEquipmentAdjustments @period=$currentDuration}
 + {@ExtractionAndRefiningEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.28 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@PipeLines @period=$currentInstant}; 
 $addend = ({@PipeLines @period=$priorInstant}
 + {@PipelinesAdditions @period=$currentDuration}
 - {@PipelinesRetirements @period=$currentDuration}
 + {@PipelinesAdjustments @period=$currentDuration}
 + {@PipelinesTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.29 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@ExtractedProductStorageEquipment @period=$currentInstant}; 
 $addend = ({@ExtractedProductStorageEquipment @period=$priorInstant}
 + {@ExtractedProductsStorageEquipmentAdditions @period=$currentDuration}
 - {@ExtractedProductsStorageEquipmentRetirements @period=$currentDuration}
 + {@ExtractedProductsStorageEquipmentAdjustments @period=$currentDuration}
 + {@ExtractedProductsStorageEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.30 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@CompressorEquipmentProductsExtractionPlant @period=$currentInstant}; 
 $addend = ({@CompressorEquipmentProductsExtractionPlant @period=$priorInstant}
 + {@CompressorEquipmentProductsExtractionPlantAdditions @period=$currentDuration}
 - {@CompressorEquipmentProductsExtractionPlantRetirements @period=$currentDuration}
 + {@CompressorEquipmentProductsExtractionPlantAdjustments @period=$currentDuration}
 + {@CompressorEquipmentProductsExtractionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.31 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@GasMeasuringAndRegulatingEquipment @period=$currentInstant}; 
 $addend = ({@GasMeasuringAndRegulatingEquipment @period=$priorInstant}
 + {@GasMeasuringAndRegulatingEquipmentAdditions @period=$currentDuration}
 - {@GasMeasuringAndRegulatingEquipmentRetirements @period=$currentDuration}
 + {@GasMeasuringAndRegulatingEquipmentAdjustments @period=$currentDuration}
 + {@GasMeasuringAndRegulatingEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.32 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherEquipmentProductsExtractionPlant @period=$currentInstant}; 
 $addend = ({@OtherEquipmentProductsExtractionPlant @period=$priorInstant}
 + {@OtherEquipmentProductsExtractionPlantAdditions @period=$currentDuration}
 - {@OtherEquipmentProductsExtractionPlantRetirements @period=$currentDuration}
 + {@OtherEquipmentProductsExtractionPlantAdjustments @period=$currentDuration}
 + {@OtherEquipmentProductsExtractionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.33 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@AssetRetirementCostsForProductsExtractionPlant @period=$currentInstant}; 
 $addend = ({@AssetRetirementCostsForProductsExtractionPlant @period=$priorInstant}
 + {@AssetRetirementCostsForProductsExtractionPlantAdditions @period=$currentDuration}
 - {@AssetRetirementCostsForProductsExtractionPlantRetirements @period=$currentDuration}
 + {@AssetRetirementCostsForProductsExtractionPlantAdjustments @period=$currentDuration}
 + {@AssetRetirementCostsForProductsExtractionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.34 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@ProductsExtractionPlant}; 
 $addend = ({@LandAndLandRightsProductsExtractionPlant}
 + {@StructuresAndImprovementsProductsExtractionPlant}
 + {@ExtractionAndRefiningEquipment}
 + {@PipeLines}
 + {@ExtractedProductStorageEquipment}
 + {@CompressorEquipmentProductsExtractionPlant}
 + {@GasMeasuringAndRegulatingEquipment}
 + {@OtherEquipmentProductsExtractionPlant}
 + {@AssetRetirementCostsForProductsExtractionPlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.35 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@NaturalGasProductionPlant}; 
 $addend = ({@NaturalGasProductionAndGatheringPlant}
 + {@ProductsExtractionPlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.36 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@ManufacturedGasProductionPlant @period=$currentInstant}; 
 $addend = ({@ManufacturedGasProductionPlant @period=$priorInstant}
 + {@ManufacturedGasProductionPlantAdditions @period=$currentDuration}
 - {@ManufacturedGasProductionPlantRetirements @period=$currentDuration}
 + {@ManufacturedGasProductionPlantAdjustments @period=$currentDuration}
 + {@ManufacturedGasProductionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.37 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@NaturalGasProductionPlantAndManufacturedGasProductionPlant}; 
 $addend = ({@NaturalGasProductionPlant}
 + {@ManufacturedGasProductionPlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.38 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@Land @period=$currentInstant}; 
 $addend = ({@Land @period=$priorInstant}
 + {@LandAdditions @period=$currentDuration}
 - {@LandRetirements @period=$currentDuration}
 + {@LandAdjustments @period=$currentDuration}
 + {@LandTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.39 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@RightsOfWayNaturalGasStorageAndProcessingPlant @period=$currentInstant}; 
 $addend = ({@RightsOfWayNaturalGasStorageAndProcessingPlant @period=$priorInstant}
 + {@RightsOfWayNaturalGasStorageAndProcessingPlantAdditions @period=$currentDuration}
 - {@RightsOfWayNaturalGasStorageAndProcessingPlantRetirements @period=$currentDuration}
 + {@RightsOfWayNaturalGasStorageAndProcessingPlantAdjustments @period=$currentDuration}
 + {@RightsOfWayNaturalGasStorageAndProcessingPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.40 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@StructuresAndImprovementsUndergroundStoragePlant @period=$currentInstant}; 
 $addend = ({@StructuresAndImprovementsUndergroundStoragePlant @period=$priorInstant}
 + {@StructuresAndImprovementsUndergroundStoragePlantAdditions @period=$currentDuration}
 - {@StructuresAndImprovementsUndergroundStoragePlantRetirements @period=$currentDuration}
 + {@StructuresAndImprovementsUndergroundStoragePlantAdjustments @period=$currentDuration}
 + {@StructuresAndImprovementsUndergroundStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.41 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@Wells @period=$currentInstant}; 
 $addend = ({@Wells @period=$priorInstant}
 + {@WellsAdditions @period=$currentDuration}
 - {@WellsRetirements @period=$currentDuration}
 + {@WellsAdjustments @period=$currentDuration}
 + {@WellsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.42 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@StorageLeaseholdsAndRights @period=$currentInstant}; 
 $addend = ({@StorageLeaseholdsAndRights @period=$priorInstant}
 + {@StorageLeaseholdsAndRightsAdditions @period=$currentDuration}
 - {@StorageLeaseholdsAndRightsRetirements @period=$currentDuration}
 + {@StorageLeaseholdsAndRightsAdjustments @period=$currentDuration}
 + {@StorageLeaseholdsAndRightsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.43 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@Reservoirs @period=$currentInstant}; 
 $addend = ({@Reservoirs @period=$priorInstant}
 + {@ReservoirsAdditions @period=$currentDuration}
 - {@ReservoirsRetirements @period=$currentDuration}
 + {@ReservoirsAdjustments @period=$currentDuration}
 + {@ReservoirsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.44 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@NonrecoverableNaturalGas @period=$currentInstant}; 
 $addend = ({@NonrecoverableNaturalGas @period=$priorInstant}
 + {@NonrecoverableNaturalGasAdditions @period=$currentDuration}
 - {@NonrecoverableNaturalGasRetirements @period=$currentDuration}
 + {@NonrecoverableNaturalGasAdjustments @period=$currentDuration}
 + {@NonrecoverableNaturalGasTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.45 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@Lines @period=$currentInstant}; 
 $addend = ({@Lines @period=$priorInstant}
 + {@LinesAdditions @period=$currentDuration}
 - {@LinesRetirements @period=$currentDuration}
 + {@LinesAdjustments @period=$currentDuration}
 + {@LinesTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.46 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@CompressorStationEquipmentUndergroundStoragePlant @period=$currentInstant}; 
 $addend = ({@CompressorStationEquipmentUndergroundStoragePlant @period=$priorInstant}
 + {@CompressorStationEquipmentUndergroundStoragePlantAdditions @period=$currentDuration}
 - {@CompressorStationEquipmentUndergroundStoragePlantRetirements @period=$currentDuration}
 + {@CompressorStationEquipmentUndergroundStoragePlantAdjustments @period=$currentDuration}
 + {@CompressorStationEquipmentUndergroundStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.47 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MeasuringAndRegulatingEquipmentUndergroundStoragePlant @period=$currentInstant}; 
 $addend = ({@MeasuringAndRegulatingEquipmentUndergroundStoragePlant @period=$priorInstant}
 + {@MeasuringAndRegulatingEquipmentUndergroundStoragePlantAdditions @period=$currentDuration}
 - {@MeasuringAndRegulatingEquipmentUndergroundStoragePlantRetirements @period=$currentDuration}
 + {@MeasuringAndRegulatingEquipmentUndergroundStoragePlantAdjustments @period=$currentDuration}
 + {@MeasuringAndRegulatingEquipmentUndergroundStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.48 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@PurificationEquipmentUndergroundStoragePlant @period=$currentInstant}; 
 $addend = ({@PurificationEquipmentUndergroundStoragePlant @period=$priorInstant}
 + {@PurificationEquipmentUndergroundStoragePlantAdditions @period=$currentDuration}
 - {@PurificationEquipmentUndergroundStoragePlantRetirements @period=$currentDuration}
 + {@PurificationEquipmentUndergroundStoragePlantAdjustments @period=$currentDuration}
 + {@PurificationEquipmentUndergroundStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.49 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherEquipmentUndergroundStoragePlant @period=$currentInstant}; 
 $addend = ({@OtherEquipmentUndergroundStoragePlant @period=$priorInstant}
 + {@OtherEquipmentUndergroundStoragePlantAdditions @period=$currentDuration}
 - {@OtherEquipmentUndergroundStoragePlantRetirements @period=$currentDuration}
 + {@OtherEquipmentUndergroundStoragePlantAdjustments @period=$currentDuration}
 + {@OtherEquipmentUndergroundStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.50 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@AssetRetirementCostsForUndergroundStoragePlant @period=$currentInstant}; 
 $addend = ({@AssetRetirementCostsForUndergroundStoragePlant @period=$priorInstant}
 + {@AssetRetirementCostsForUndergroundStoragePlantAdditions @period=$currentDuration}
 - {@AssetRetirementCostsForUndergroundStoragePlantRetirements @period=$currentDuration}
 + {@AssetRetirementCostsForUndergroundStoragePlantAdjustments @period=$currentDuration}
 + {@AssetRetirementCostsForUndergroundStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.51 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@UndergroundStoragePlant}; 
 $addend = ({@Land}
 + {@RightsOfWayNaturalGasStorageAndProcessingPlant}
 + {@StructuresAndImprovementsUndergroundStoragePlant}
 + {@Wells}
 + {@StorageLeaseholdsAndRights}
 + {@Reservoirs}
 + {@NonrecoverableNaturalGas}
 + {@Lines}
 + {@CompressorStationEquipmentUndergroundStoragePlant}
 + {@MeasuringAndRegulatingEquipmentUndergroundStoragePlant}
 + {@PurificationEquipmentUndergroundStoragePlant}
 + {@OtherEquipmentUndergroundStoragePlant}
 + {@AssetRetirementCostsForUndergroundStoragePlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported a value for UndergroundStoragePlant with a value of {$sum1} that is not equal to the value of the sum of its components of {$addend}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.52 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@LandAndLandRightsOtherStoragePlant @period=$currentInstant}; 
 $addend = ({@LandAndLandRightsOtherStoragePlant @period=$priorInstant}
 + {@LandAndLandRightsOtherStoragePlantAdditions @period=$currentDuration}
 - {@LandAndLandRightsOtherStoragePlantRetirements @period=$currentDuration}
 + {@LandAndLandRightsOtherStoragePlantAdjustments @period=$currentDuration}
 + {@LandAndLandRightsOtherStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.53 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@StructuresAndImprovementsOtherStoragePlant @period=$currentInstant}; 
 $addend = ({@StructuresAndImprovementsOtherStoragePlant @period=$priorInstant}
 + {@StructuresAndImprovementsOtherStoragePlantAdditions @period=$currentDuration}
 - {@StructuresAndImprovementsOtherStoragePlantRetirements @period=$currentDuration}
 + {@StructuresAndImprovementsOtherStoragePlantAdjustments @period=$currentDuration}
 + {@StructuresAndImprovementsOtherStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.54 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@GasHolders @period=$currentInstant}; 
 $addend = ({@GasHolders @period=$priorInstant}
 + {@GasHoldersAdditions @period=$currentDuration}
 - {@GasHoldersRetirements @period=$currentDuration}
 + {@GasHoldersAdjustments @period=$currentDuration}
 + {@GasHoldersTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.55 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@PurificationEquipmentOtherStoragePlant @period=$currentInstant}; 
 $addend = ({@PurificationEquipmentOtherStoragePlant @period=$priorInstant}
 + {@PurificationEquipmentOtherStoragePlantAdditions @period=$currentDuration}
 - {@PurificationEquipmentOtherStoragePlantRetirements @period=$currentDuration}
 + {@PurificationEquipmentOtherStoragePlantAdjustments @period=$currentDuration}
 + {@PurificationEquipmentOtherStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.56 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@LiquefactionEquipment @period=$currentInstant}; 
 $addend = ({@LiquefactionEquipment @period=$priorInstant}
 + {@LiquefactionEquipmentAdditions @period=$currentDuration}
 - {@LiquefactionEquipmentRetirements @period=$currentDuration}
 + {@LiquefactionEquipmentAdjustments @period=$currentDuration}
 + {@LiquefactionEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.57 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@VaporizingEquipment @period=$currentInstant}; 
 $addend = ({@VaporizingEquipment @period=$priorInstant}
 + {@VaporizingEquipmentAdditions @period=$currentDuration}
 - {@VaporizingEquipmentRetirements @period=$currentDuration}
 + {@VaporizingEquipmentAdjustments @period=$currentDuration}
 + {@VaporizingEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.58 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@CompressorEquipmentOtherStoragePlant @period=$currentInstant}; 
 $addend = ({@CompressorEquipmentOtherStoragePlant @period=$priorInstant}
 + {@CompressorEquipmentOtherStoragePlantAdditions @period=$currentDuration}
 - {@CompressorEquipmentOtherStoragePlantRetirements @period=$currentDuration}
 + {@CompressorEquipmentOtherStoragePlantAdjustments @period=$currentDuration}
 + {@CompressorEquipmentOtherStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.59 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MeasuringAndRegulatingEquipmentOtherStoragePlant @period=$currentInstant}; 
 $addend = ({@MeasuringAndRegulatingEquipmentOtherStoragePlant @period=$priorInstant}
 + {@MeasuringAndRegulatingEquipmentOtherStoragePlantAdditions @period=$currentDuration}
 - {@MeasuringAndRegulatingEquipmentOtherStoragePlantRetirements @period=$currentDuration}
 + {@MeasuringAndRegulatingEquipmentOtherStoragePlantAdjustments @period=$currentDuration}
 + {@MeasuringAndRegulatingEquipmentOtherStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.60 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherEquipmentOtherStoragePlant @period=$currentInstant}; 
 $addend = ({@OtherEquipmentOtherStoragePlant @period=$priorInstant}
 + {@OtherEquipmentOtherStoragePlantAdditions @period=$currentDuration}
 - {@OtherEquipmentOtherStoragePlantRetirements @period=$currentDuration}
 + {@OtherEquipmentOtherStoragePlantAdjustments @period=$currentDuration}
 + {@OtherEquipmentOtherStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.61 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@AssetRetirementCostsForOtherStoragePlant @period=$currentInstant}; 
 $addend = ({@AssetRetirementCostsForOtherStoragePlant @period=$priorInstant}
 + {@AssetRetirementCostsForOtherStoragePlantAdditions @period=$currentDuration}
 - {@AssetRetirementCostsForOtherStoragePlantRetirements @period=$currentDuration}
 + {@AssetRetirementCostsForOtherStoragePlantAdjustments @period=$currentDuration}
 + {@AssetRetirementCostsForOtherStoragePlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.62 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherStoragePlant}; 
 $addend = ({@LandAndLandRightsOtherStoragePlant}
 + {@StructuresAndImprovementsOtherStoragePlant}
 + {@GasHolders}
 + {@PurificationEquipmentOtherStoragePlant}
 + {@LiquefactionEquipment}
 + {@VaporizingEquipment}
 + {@CompressorEquipmentOtherStoragePlant}
 + {@MeasuringAndRegulatingEquipmentOtherStoragePlant}
 + {@OtherEquipmentOtherStoragePlant}
 + {@AssetRetirementCostsForOtherStoragePlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported a value for OtherStoragePlant with a value of {$sum1} that is not equal to the value of the sum of its components of {$addend}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.63 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@LandAndLandRightsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$currentInstant}; 
 $addend = ({@LandAndLandRightsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$priorInstant}
 + {@LandAndLandRightsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdditions @period=$currentDuration}
 - {@LandAndLandRightsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantRetirements @period=$currentDuration}
 + {@LandAndLandRightsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdjustments @period=$currentDuration}
 + {@LandAndLandRightsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.64 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@StructuresAndImprovementsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$currentInstant}; 
 $addend = ({@StructuresAndImprovementsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$priorInstant}
 + {@StructuresAndImprovementsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdditions @period=$currentDuration}
 - {@StructuresAndImprovementsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantRetirements @period=$currentDuration}
 + {@StructuresAndImprovementsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdjustments @period=$currentDuration}
 + {@StructuresAndImprovementsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.65 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@LngProcessingTerminalEquipment @period=$currentInstant}; 
 $addend = ({@LngProcessingTerminalEquipment @period=$priorInstant}
 + {@LngProcessingTerminalEquipmentAdditions @period=$currentDuration}
 - {@LngProcessingTerminalEquipmentRetirements @period=$currentDuration}
 + {@LngProcessingTerminalEquipmentAdjustments @period=$currentDuration}
 + {@LngProcessingTerminalEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.66 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@LngTransportationEquipment @period=$currentInstant}; 
 $addend = ({@LngTransportationEquipment @period=$priorInstant}
 + {@LngTransportationEquipmentAdditions @period=$currentDuration}
 - {@LngTransportationEquipmentRetirements @period=$currentDuration}
 + {@LngTransportationEquipmentAdjustments @period=$currentDuration}
 + {@LngTransportationEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.67 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MeasuringAndRegulatingEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$currentInstant}; 
 $addend = ({@MeasuringAndRegulatingEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$priorInstant}
 + {@MeasuringAndRegulatingEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdditions @period=$currentDuration}
 - {@MeasuringAndRegulatingEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantRetirements @period=$currentDuration}
 + {@MeasuringAndRegulatingEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdjustments @period=$currentDuration}
 + {@MeasuringAndRegulatingEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.68 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@CompressorStationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$currentInstant}; 
 $addend = ({@CompressorStationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$priorInstant}
 + {@CompressorStationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdditions @period=$currentDuration}
 - {@CompressorStationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantRetirements @period=$currentDuration}
 + {@CompressorStationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdjustments @period=$currentDuration}
 + {@CompressorStationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.69 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@CommunicationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$currentInstant}; 
 $addend = ({@CommunicationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$priorInstant}
 + {@CommunicationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdditions @period=$currentDuration}
 - {@CommunicationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantRetirements @period=$currentDuration}
 + {@CommunicationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdjustments @period=$currentDuration}
 + {@CommunicationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.70 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$currentInstant}; 
 $addend = ({@OtherEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$priorInstant}
 + {@OtherEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdditions @period=$currentDuration}
 - {@OtherEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantRetirements @period=$currentDuration}
 + {@OtherEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdjustments @period=$currentDuration}
 + {@OtherEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.71 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@AssetRetirementCostsForBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$currentInstant}; 
 $addend = ({@AssetRetirementCostsForBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant @period=$priorInstant}
 + {@AssetRetirementCostsForBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdditions @period=$currentDuration}
 - {@AssetRetirementCostsForBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantRetirements @period=$currentDuration}
 + {@AssetRetirementCostsForBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantAdjustments @period=$currentDuration}
 + {@AssetRetirementCostsForBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.72 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@BaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant}; 
 $addend = ({@LandAndLandRightsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant}
 + {@StructuresAndImprovementsBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant}
 + {@LngProcessingTerminalEquipment}
 + {@LngTransportationEquipment}
 + {@MeasuringAndRegulatingEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant}
 + {@CompressorStationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant}
 + {@CommunicationEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant}
 + {@OtherEquipmentBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant}
 + {@AssetRetirementCostsForBaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported a value for BaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant with a value of {$sum1} that is not equal to the value of the sum of its components of {$addend}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.73 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@NaturalGasStorageAndProcessingPlant}; 
 $addend = ({@UndergroundStoragePlant}
 + {@OtherStoragePlant}
 + {@BaseLoadLiquefiedNaturalGasTerminalingAndProcessingPlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported a value for NaturalGasStorageAndProcessingPlant with a value of {$sum1} that is not equal to the value of the sum of its components of {$addend}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.74 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@LandAndLandRightsGasTransmissionPlant @period=$currentInstant}; 
 $addend = ({@LandAndLandRightsGasTransmissionPlant @period=$priorInstant}
 + {@LandAndLandRightsGasTransmissionPlantAdditions @period=$currentDuration}
 - {@LandAndLandRightsGasTransmissionPlantRetirements @period=$currentDuration}
 + {@LandAndLandRightsGasTransmissionPlantAdjustments @period=$currentDuration}
 + {@LandAndLandRightsGasTransmissionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.75 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@RightsOfWayGasTransmissionPlant @period=$currentInstant}; 
 $addend = ({@RightsOfWayGasTransmissionPlant @period=$priorInstant}
 + {@RightsOfWayGasTransmissionPlantAdditions @period=$currentDuration}
 - {@RightsOfWayGasTransmissionPlantRetirements @period=$currentDuration}
 + {@RightsOfWayGasTransmissionPlantAdjustments @period=$currentDuration}
 + {@RightsOfWayGasTransmissionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.76 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@StructuresAndImprovementsGasTransmissionPlant @period=$currentInstant}; 
 $addend = ({@StructuresAndImprovementsGasTransmissionPlant @period=$priorInstant}
 + {@StructuresAndImprovementsGasTransmissionPlantAdditions @period=$currentDuration}
 - {@StructuresAndImprovementsGasTransmissionPlantRetirements @period=$currentDuration}
 + {@StructuresAndImprovementsGasTransmissionPlantAdjustments @period=$currentDuration}
 + {@StructuresAndImprovementsGasTransmissionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.77 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MainsGasTransmissionPlant @period=$currentInstant}; 
 $addend = ({@MainsGasTransmissionPlant @period=$priorInstant}
 + {@MainsGasTransmissionPlantAdditions @period=$currentDuration}
 - {@MainsGasTransmissionPlantRetirements @period=$currentDuration}
 + {@MainsGasTransmissionPlantAdjustments @period=$currentDuration}
 + {@MainsGasTransmissionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.78 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@CompressorStationEquipmentGasTransmissionPlant @period=$currentInstant}; 
 $addend = ({@CompressorStationEquipmentGasTransmissionPlant @period=$priorInstant}
 + {@CompressorStationEquipmentGasTransmissionPlantAdditions @period=$currentDuration}
 - {@CompressorStationEquipmentGasTransmissionPlantRetirements @period=$currentDuration}
 + {@CompressorStationEquipmentGasTransmissionPlantAdjustments @period=$currentDuration}
 + {@CompressorStationEquipmentGasTransmissionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.79 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MeasuringAndRegulatingStationEquipment @period=$currentInstant}; 
 $addend = ({@MeasuringAndRegulatingStationEquipment @period=$priorInstant}
 + {@MeasuringAndRegulatingStationEquipmentAdditions @period=$currentDuration}
 - {@MeasuringAndRegulatingStationEquipmentRetirements @period=$currentDuration}
 + {@MeasuringAndRegulatingStationEquipmentAdjustments @period=$currentDuration}
 + {@MeasuringAndRegulatingStationEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.80 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@CommunicationEquipmentTransmission @period=$currentInstant}; 
 $addend = ({@CommunicationEquipmentTransmission @period=$priorInstant}
 + {@CommunicationEquipmentTransmissionAdditions @period=$currentDuration}
 - {@CommunicationEquipmentTransmissionRetirements @period=$currentDuration}
 + {@CommunicationEquipmentTransmissionAdjustments @period=$currentDuration}
 + {@CommunicationEquipmentTransmissionTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.81 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherEquipmentGasTransmissionPlant @period=$currentInstant}; 
 $addend = ({@OtherEquipmentGasTransmissionPlant @period=$priorInstant}
 + {@OtherEquipmentGasTransmissionPlantAdditions @period=$currentDuration}
 - {@OtherEquipmentGasTransmissionPlantRetirements @period=$currentDuration}
 + {@OtherEquipmentGasTransmissionPlantAdjustments @period=$currentDuration}
 + {@OtherEquipmentGasTransmissionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.82 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@AssetRetirementCostsForTransmissionPlantTransmissionPlant @period=$currentInstant}; 
 $addend = ({@AssetRetirementCostsForTransmissionPlantTransmissionPlant @period=$priorInstant}
 + {@AssetRetirementCostsForTransmissionPlantAdditions @period=$currentDuration}
 - {@AssetRetirementCostsForTransmissionPlantRetirements @period=$currentDuration}
 + {@AssetRetirementCostsForTransmissionPlantAdjustments @period=$currentDuration}
 + {@AssetRetirementCostsForTransmissionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.83 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@TransmissionPlant}; 
 $addend = ({@LandAndLandRightsGasTransmissionPlant}
 + {@RightsOfWayGasTransmissionPlant}
 + {@StructuresAndImprovementsGasTransmissionPlant}
 + {@MainsGasTransmissionPlant}
 + {@CompressorStationEquipmentGasTransmissionPlant}
 + {@MeasuringAndRegulatingStationEquipment}
 + {@CommunicationEquipmentTransmission}
 + {@OtherEquipmentGasTransmissionPlant}
 + {@AssetRetirementCostsForTransmissionPlantTransmissionPlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported a value for TransmissionPlant with a value of {$sum1} that is not equal to the value of the sum of its components of {$addend}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.84 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@LandAndLandRightsGasDistributionPlant @period=$currentInstant}; 
 $addend = ({@LandAndLandRightsGasDistributionPlant @period=$priorInstant}
 + {@LandAndLandRightsGasDistributionPlantAdditions @period=$currentDuration}
 - {@LandAndLandRightsGasDistributionPlantRetirements @period=$currentDuration}
 + {@LandAndLandRightsGasDistributionPlantAdjustments @period=$currentDuration}
 + {@LandAndLandRightsGasDistributionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.85 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@StructuresAndImprovementsGasDistributionPlant @period=$currentInstant}; 
 $addend = ({@StructuresAndImprovementsGasDistributionPlant @period=$priorInstant}
 + {@StructuresAndImprovementsGasDistributionPlantAdditions @period=$currentDuration}
 - {@StructuresAndImprovementsGasDistributionPlantRetirements @period=$currentDuration}
 + {@StructuresAndImprovementsGasDistributionPlantAdjustments @period=$currentDuration}
 + {@StructuresAndImprovementsGasDistributionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.86 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MainsGasDistributionPlant @period=$currentInstant}; 
 $addend = ({@MainsGasDistributionPlant @period=$priorInstant}
 + {@MainsGasDistributionPlantAdditions @period=$currentDuration}
 - {@MainsGasDistributionPlantRetirements @period=$currentDuration}
 + {@MainsGasDistributionPlantAdjustments @period=$currentDuration}
 + {@MainsGasDistributionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.87 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@CompressorStationEquipmentGasDistributionPlant @period=$currentInstant}; 
 $addend = ({@CompressorStationEquipmentGasDistributionPlant @period=$priorInstant}
 + {@CompressorStationEquipmentGasDistributionPlantAdditions @period=$currentDuration}
 - {@CompressorStationEquipmentGasDistributionPlantRetirements @period=$currentDuration}
 + {@CompressorStationEquipmentGasDistributionPlantAdjustments @period=$currentDuration}
 + {@CompressorStationEquipmentGasDistributionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.88 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MeasuringAndRegulatingStationEquipmentGeneral @period=$currentInstant}; 
 $addend = ({@MeasuringAndRegulatingStationEquipmentGeneral @period=$priorInstant}
 + {@MeasuringAndRegulatingStationEquipmentGeneralAdditions @period=$currentDuration}
 - {@MeasuringAndRegulatingStationEquipmentGeneralRetirements @period=$currentDuration}
 + {@MeasuringAndRegulatingStationEquipmentGeneralAdjustments @period=$currentDuration}
 + {@MeasuringAndRegulatingStationEquipmentGeneralTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.89 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MeasuringAndRegulatingStationEquipmentCityGateCheckStations @period=$currentInstant}; 
 $addend = ({@MeasuringAndRegulatingStationEquipmentCityGateCheckStations @period=$priorInstant}
 + {@MeasuringAndRegulatingStationEquipmentCityGateCheckStationsAdditions @period=$currentDuration}
 - {@MeasuringAndRegulatingStationEquipmentCityGateCheckStationsRetirements @period=$currentDuration}
 + {@MeasuringAndRegulatingStationEquipmentCityGateCheckStationsAdjustments @period=$currentDuration}
 + {@MeasuringAndRegulatingStationEquipmentCityGateCheckStationsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.90 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@Services @period=$currentInstant}; 
 $addend = ({@Services @period=$priorInstant}
 + {@ServicesAdditions @period=$currentDuration}
 - {@ServicesRetirements @period=$currentDuration}
 + {@ServicesAdjustments @period=$currentDuration}
 + {@ServicesTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.91 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@Meters @period=$currentInstant}; 
 $addend = ({@Meters @period=$priorInstant}
 + {@MetersAdditions @period=$currentDuration}
 - {@MetersRetirements @period=$currentDuration}
 + {@MetersAdjustments @period=$currentDuration}
 + {@MetersTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.92 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MeterInstallations @period=$currentInstant}; 
 $addend = ({@MeterInstallations @period=$priorInstant}
 + {@MeterInstallationsAdditions @period=$currentDuration}
 - {@MeterInstallationsRetirements @period=$currentDuration}
 + {@MeterInstallationsAdjustments @period=$currentDuration}
 + {@MeterInstallationsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.93 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@HouseRegulators @period=$currentInstant}; 
 $addend = ({@HouseRegulators @period=$priorInstant}
 + {@HouseRegulatorsAdditions @period=$currentDuration}
 - {@HouseRegulatorsRetirements @period=$currentDuration}
 + {@HouseRegulatorsAdjustments @period=$currentDuration}
 + {@HouseRegulatorsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.94 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@HouseRegulatoryInstallations @period=$currentInstant}; 
 $addend = ({@HouseRegulatoryInstallations @period=$priorInstant}
 + {@HouseRegulatoryInstallationsAdditions @period=$currentDuration}
 - {@HouseRegulatoryInstallationsRetirements @period=$currentDuration}
 + {@HouseRegulatoryInstallationsAdjustments @period=$currentDuration}
 + {@HouseRegulatoryInstallationsTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.95 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@IndustrialMeasuringAndRegulatingStationEquipment @period=$currentInstant}; 
 $addend = ({@IndustrialMeasuringAndRegulatingStationEquipment @period=$priorInstant}
 + {@IndustrialMeasuringAndRegulatingStationEquipmentAdditions @period=$currentDuration}
 - {@IndustrialMeasuringAndRegulatingStationEquipmentRetirements @period=$currentDuration}
 + {@IndustrialMeasuringAndRegulatingStationEquipmentAdjustments @period=$currentDuration}
 + {@IndustrialMeasuringAndRegulatingStationEquipmentTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.96 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherPropertyOnCustomersPremises @period=$currentInstant}; 
 $addend = ({@OtherPropertyOnCustomersPremises @period=$priorInstant}
 + {@OtherPropertyOnCustomersPremisesAdditions @period=$currentDuration}
 - {@OtherPropertyOnCustomersPremisesRetirements @period=$currentDuration}
 + {@OtherPropertyOnCustomersPremisesAdjustments @period=$currentDuration}
 + {@OtherPropertyOnCustomersPremisesTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.97 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OtherEquipmentGasDistributionPlant @period=$currentInstant}; 
 $addend = ({@OtherEquipmentGasDistributionPlant @period=$priorInstant}
 + {@OtherEquipmentGasDistributionPlantAdditions @period=$currentDuration}
 - {@OtherEquipmentGasDistributionPlantRetirements @period=$currentDuration}
 + {@OtherEquipmentGasDistributionPlantAdjustments @period=$currentDuration}
 + {@OtherEquipmentGasDistributionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.98 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@AssetRetirementCostsForDistributionPlantDistributionPlant @period=$currentInstant}; 
 $addend = ({@AssetRetirementCostsForDistributionPlantDistributionPlant @period=$priorInstant}
 + {@AssetRetirementCostsForDistributionPlantDistributionPlantAdditions @period=$currentDuration}
 - {@AssetRetirementCostsForDistributionPlantDistributionPlantRetirements @period=$currentDuration}
 + {@AssetRetirementCostsForDistributionPlantDistributionPlantAdjustments @period=$currentDuration}
 + {@AssetRetirementCostsForDistributionPlantDistributionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.99 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@DistributionPlant}; 
 $addend = ({@LandAndLandRightsGasDistributionPlant}
 + {@StructuresAndImprovementsGasDistributionPlant}
 + {@MainsGasDistributionPlant}
 + {@CompressorStationEquipmentGasDistributionPlant}
 + {@MeasuringAndRegulatingStationEquipmentGeneral}
 + {@MeasuringAndRegulatingStationEquipmentCityGateCheckStations}
 + {@Services}
 + {@Meters}
 + {@MeterInstallations}
 + {@HouseRegulators}
 + {@HouseRegulatoryInstallations}
 + {@IndustrialMeasuringAndRegulatingStationEquipment}
 + {@OtherPropertyOnCustomersPremises}
 + {@OtherEquipmentGasDistributionPlant}
 + {@AssetRetirementCostsForDistributionPlantDistributionPlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported a value for DistributionPlant with a value of {$sum1} that is not equal to the value of the sum of its components of {$addend}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.100 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@LandAndLandRights @period=$currentInstant}; 
 $addend = ({@LandAndLandRights @period=$priorInstant}
 + {@LandAndLandRightsGeneralPlantAdditions @period=$currentDuration}
 - {@LandAndLandRightsGeneralPlantRetirements @period=$currentDuration}
 + {@LandAndLandRightsGeneralPlantAdjustments @period=$currentDuration}
 + {@LandAndLandRightsGeneralPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.101 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@StructuresAndImprovements @period=$currentInstant}; 
 $addend = ({@StructuresAndImprovements @period=$priorInstant}
 + {@StructuresAndImprovementsGeneralPlantAdditions @period=$currentDuration}
 - {@StructuresAndImprovementsGeneralPlantRetirements @period=$currentDuration}
 + {@StructuresAndImprovementsGeneralPlantAdjustments @period=$currentDuration}
 + {@StructuresAndImprovementsGeneralPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.102 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@OfficeFurnitureAndEquipment @period=$currentInstant}; 
 $addend = ({@OfficeFurnitureAndEquipment @period=$priorInstant}
 + {@OfficeFurnitureAndEquipmentGeneralPlantAdditions @period=$currentDuration}
 - {@OfficeFurnitureAndEquipmentGeneralPlantRetirements @period=$currentDuration}
 + {@OfficeFurnitureAndEquipmentGeneralPlantAdjustments @period=$currentDuration}
 + {@OfficeFurnitureAndEquipmentGeneralPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.103 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@TransportationEquipment @period=$currentInstant}; 
 $addend = ({@TransportationEquipment @period=$priorInstant}
 + {@TransportationEquipmentGeneralPlantAdditions @period=$currentDuration}
 - {@TransportationEquipmentGeneralPlantRetirements @period=$currentDuration}
 + {@TransportationEquipmentGeneralPlantAdjustments @period=$currentDuration}
 + {@TransportationEquipmentGeneralPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.104 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@StoresEquipment @period=$currentInstant}; 
 $addend = ({@StoresEquipment @period=$priorInstant}
 + {@StoresEquipmentGeneralPlantAdditions @period=$currentDuration}
 - {@StoresEquipmentGeneralPlantRetirements @period=$currentDuration}
 + {@StoresEquipmentGeneralPlantAdjustments @period=$currentDuration}
 + {@StoresEquipmentGeneralPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.105 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@ToolsShopAndGarageEquipment @period=$currentInstant}; 
 $addend = ({@ToolsShopAndGarageEquipment @period=$priorInstant}
 + {@ToolsShopAndGarageEquipmentGeneralPlantAdditions @period=$currentDuration}
 - {@ToolsShopAndGarageEquipmentGeneralPlantRetirements @period=$currentDuration}
 + {@ToolsShopAndGarageEquipmentGeneralPlantAdjustments @period=$currentDuration}
 + {@ToolsShopAndGarageEquipmentGeneralPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.106 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@LaboratoryEquipment @period=$currentInstant}; 
 $addend = ({@LaboratoryEquipment @period=$priorInstant}
 + {@LaboratoryEquipmentGeneralPlantAdditions @period=$currentDuration}
 - {@LaboratoryEquipmentGeneralPlantRetirements @period=$currentDuration}
 + {@LaboratoryEquipmentGeneralPlantAdjustments @period=$currentDuration}
 + {@LaboratoryEquipmentGeneralPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.107 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@PowerOperatedEquipment @period=$currentInstant}; 
 $addend = ({@PowerOperatedEquipment @period=$priorInstant}
 + {@PowerOperatedEquipmentGeneralPlantAdditions @period=$currentDuration}
 - {@PowerOperatedEquipmentGeneralPlantRetirements @period=$currentDuration}
 + {@PowerOperatedEquipmentGeneralPlantAdjustments @period=$currentDuration}
 + {@PowerOperatedEquipmentGeneralPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.108 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@CommunicationEquipment @period=$currentInstant}; 
 $addend = ({@CommunicationEquipment @period=$priorInstant}
 + {@CommunicationEquipmentGeneralPlantAdditions @period=$currentDuration}
 - {@CommunicationEquipmentGeneralPlantRetirements @period=$currentDuration}
 + {@CommunicationEquipmentGeneralPlantAdjustments @period=$currentDuration}
 + {@CommunicationEquipmentGeneralPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.109 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@MiscellaneousEquipment @period=$currentInstant}; 
 $addend = ({@MiscellaneousEquipment @period=$priorInstant}
 + {@MiscellaneousEquipmentGeneralPlantAdditions @period=$currentDuration}
 - {@MiscellaneousEquipmentGeneralPlantRetirements @period=$currentDuration}
 + {@MiscellaneousEquipmentGeneralPlantAdjustments @period=$currentDuration}
 + {@MiscellaneousEquipmentGeneralPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.110 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@GeneralPlantExcludingOtherTangiblePropertyAndAssetRetirementCostsForGeneralPlant}; 
 $addend = ({@LandAndLandRights}
 + {@StructuresAndImprovements}
 + {@OfficeFurnitureAndEquipment}
 + {@TransportationEquipment}
 + {@StoresEquipment}
 + {@ToolsShopAndGarageEquipment}
 + {@LaboratoryEquipment}
 + {@PowerOperatedEquipment}
 + {@CommunicationEquipment}
 + {@MiscellaneousEquipment});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported a value for GeneralPlantExcludingOtherTangiblePropertyAndAssetRetirementCostsForGeneralPlant with a value of {$sum1} that is not equal to the value of the sum of its components of {$addend}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.111 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@GeneralPlant}; 
 $addend = ({@GeneralPlantExcludingOtherTangiblePropertyAndAssetRetirementCostsForGeneralPlant}
 + {@OtherTangibleProperty}
 + {@AssetRetirementCostsForGeneralPlantGeneralPlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported a value for GeneralPlant with a value of {$sum1} that is not equal to the value of the sum of its components of {$addend}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.112 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@GasPlantInServiceAndCompletedConstructionNotClassifiedGas}; 
 $addend = ({@IntangiblePlant}
 + {@NaturalGasProductionPlantAndManufacturedGasProductionPlant}
 + {@NaturalGasStorageAndProcessingPlant}
 + {@TransmissionPlant}
 + {@DistributionPlant}
 + {@GeneralPlant});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported a value for GasPlantInServiceAndCompletedConstructionNotClassifiedGas with a value of {$sum1} that is not equal to the value of the sum of its components of {$addend}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.113 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@GasPlantInService}; 
 $addend = ({@GasPlantInServiceAndCompletedConstructionNotClassifiedGas}
 + {@GasPlantPurchased}
 - {@GasPlantSold}
 + {@ExperimentalGasPlantUnclassified});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported a value for GasPlantInService with a value of {$sum1} that is not equal to the value of the sum of its components of {$addend}

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1

assert F2.204.114 satisfied 
$rule_id = rule-name();
if ($waivedSchedules intersect $scheduleLookup[(rule-name().split('.'))[3]]).length > 0
        skip
else

if set('2/3-Q').contains([covered @concept.local-name ='FormType'])
        skip
else
 $sum1 = {@NaturalGasProductionPlantAndManufacturedGasProductionPlant @period=$currentInstant}; 
 $addend = ({@NaturalGasProductionPlantAndManufacturedGasProductionPlant @period=$priorInstant}
 + {@NaturalGasProductionPlantAndManufacturedGasProductionPlantAdditions @period=$currentDuration}
 - {@NaturalGasProductionPlantAndManufacturedGasProductionPlantRetirements @period=$currentDuration}
 + {@NaturalGasProductionPlantAndManufacturedGasProductionPlantAdjustments @period=$currentDuration}
 + {@NaturalGasProductionPlantAndManufacturedGasProductionPlantTransfers @period=$currentDuration});
tolerance_for_decimals($sum1,$addend,$tolerance_factor)  
message 
"The company has reported an ending balance for {$sum1.concept.name.local-name} with a value of {$sum1} that is not equal to the value of opening balance of {$sum1.concept.name.local-name} plus any changes during the period. The calculated ending balance is {$addend}.

Total Element : {$sum1.concept.name}
Total Period : {$sum1.period}
Total Value : {$sum1} 

Rule Id:{$rule_id}"
severity error
rule-focus $sum1
